export interface WithPrimitivesArray {
  
  ints : Array<number>;
  
  longs : Array<number>;
  
  floats : Array<number>;
  
  doubles : Array<number>;
  
  booleans : Array<boolean>;
  
  strings : Array<string>;
  
  bytes : Array<string>;
}
