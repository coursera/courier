namespace org.coursera.maps.mutable

import org.coursera.enums.Fruits
import org.coursera.records.mutable.Empty
import org.coursera.records.mutable.Simple
import org.coursera.`fixed`.Fixed8

@android.mutability = "MUTABLE"
@swift.omit
record WithComplexTypesMap {
  empties: map[string, Empty]
  fruits: map[string, Fruits]
  arrays: map[string, array[Simple]]
  maps: map[string, map[string, Simple]]
  unions: map[string, union[int, string, Simple]]
  `fixed`: map[string, Fixed8]
}
