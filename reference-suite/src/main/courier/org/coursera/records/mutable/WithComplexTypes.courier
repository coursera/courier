namespace org.coursera.records.mutable

import org.coursera.customtypes.CustomInt
import org.coursera.enums.Fruits

@android.mutability = "MUTABLE"
@swift.omit
record WithComplexTypes {
  `record`: Simple
  `enum`: Fruits
  `union`: union[int, string, Simple]
  `array`: array[int]
  `map`: map[string, int]
  complexMap: map[string, Simple]
  custom: CustomInt
}
