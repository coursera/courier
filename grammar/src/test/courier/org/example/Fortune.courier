namespace org.example

/**
 * A fortune.
 */
record Fortune {
  /**
   * The fortune telling.
   */
  telling: FortuneTelling // comment 1
  createdAt: /* comment 2 */ org.example.common.DateTime

  @scala.omit
  keyedMap: map[Note, Note]

  `namespace`: array[string] = ["org", "example"]
  escaped: `record`
}
