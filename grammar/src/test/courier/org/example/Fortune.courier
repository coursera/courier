namespace org.example

import org.example.common.Timestamp
import org.example.common.DateTime

/**
 * A fortune.
 */
record Fortune {
  /**
   * The fortune telling.
   */
  telling: FortuneTelling
  // comment 1
   createdAt: /* comment 2 */ DateTime

  @scala.omit
  keyedMap: map[Note, Note]

  `namespace`: array[string] = ["org", "example"]
  escaped: `record`
}
